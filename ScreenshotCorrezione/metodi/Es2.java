

/* La classe ListaLimitata rappresenta una lista con capacità limitata. In questo esercizio il limite è stabilito dalla variabile
 * statica DIM_MASSIMA. Implementare il metodo add come segue:
 * * se la dimensione della lista non ha raggiunto il limite, allora il metodo aggiunge il nuovo elemento in coda alla lista
 * * se invece il numero massimo di elementi all'interno della lista è stato raggiunto allora, prima di inserire il nuovo elemento,
 * *	deve essere rimosso quello con il valore più piccolo
 * * se invece l'elemento che si sta tentando di inserire già esiste nella lista, allora quello vecchio va rimosso e il nuovo
 * *	aggiunto in coda, senza nessun controllo sulla dimensione
 * Suggerimento: il metodo List.IndexOf restituisce l'indice di un elemento all'interno di una lista
 * 			
 */

public class Es2 {

}
